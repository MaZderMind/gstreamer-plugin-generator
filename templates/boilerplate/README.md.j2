{# @formatter:off -#}
{{ plugin.name }}
{{ '=' * (plugin.name | length) }}
URL: {{ plugin.url }}
{{ plugin.description }}

Boilerplate generated with {{ generator }}
{% for element in plugin.elements %}

{{ element.name }}
{{ '-' * (element.name | length) }}
{{ element.description }}
{% endfor %}

Install Build-Dependencies
--------------------------
```
apt install -y build-essential autoconf pkg-config libtool gstreamer1.0-tools libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev
```

Getting-Started
---------------
```
cd {{ plugin.name | all_lower_case }}
git init
git submodules init
./autogen.sh

cd examples
./inspect.sh
./run.sh
```
